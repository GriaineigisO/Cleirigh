{
  "dependencies": {
    "axios": "^1.7.8",
    "bootstrap": "^5.3.3"
  },
  "scripts": {
    "start": "node backend/server.js",            // Starts the Node server
    "postinstall": "npm run build --prefix frontend"  // Builds the React app after install
  }
}
