{
  "scripts": {
    "start": "npm --prefix backend start",                      // Starts the Node.js server
    "postinstall": "npm run build --prefix frontend"             // Builds the React app after install
  },
  "dependencies": {
    "axios": "^1.7.8",
    "bootstrap": "^5.3.3"
  }
}
