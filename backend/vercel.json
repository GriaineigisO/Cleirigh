{
  "version": 2,
  "builds": [
    {
      "src": "server.js", // Path to your backend
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server.js"
    }
  ]
}
